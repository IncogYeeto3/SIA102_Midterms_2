@model IEnumerable<SIA102_Midterms_2.DTOs.TitleReadDTO>

@{
    ViewData["Title"] = "Titles";
}

<h1>Titles</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Title</th>
            <th>Type</th>
            <th>Price</th>
            <th>Pub Date</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Title1</td>
                <td>@item.Type</td>
                <td>@item.Price?.ToString("C")</td>
                <td>@item.Pubdate.ToShortDateString()</td>
                <td>
                    <a asp-action="Details" asp-route-id="@item.TitleId">Details</a> |
                    <a asp-action="Edit" asp-route-id="@item.TitleId">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.TitleId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
